#/bin/bash
TIMES=$(echo $1 | tr "," "\n")

for t in $TIMES
do
    echo $t
    ms="000$t"
    ms=${ms: -4}
    sec="${ms: -4: 1}.${ms: -3}"
    file_name="${ms}ms.log"
    #echo "./compare 100 ${file_name} -pt $sec -k 300"
    ./compare 100 ${file_name} -pt $sec -k 300
done